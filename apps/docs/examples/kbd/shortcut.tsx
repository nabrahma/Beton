/**
 * Shortcut
 * Combine keys with plain text separators.
 */
import { Kbd, Text } from "@beton-ui/react";

export default function KbdShortcut() {
  return (
    <Text>
      Open the command palette with <Kbd>Ctrl</Kbd> + <Kbd>K</Kbd>.
    </Text>
  );
}
